---
- name: adding a firewall rules
  block:
    - name: allow ospf
      firewalld:
        zone: public
        port: "{{ items }}"
        state: enabled
        permanent: yes
      loop:
        - 2049/tcp
        - 89/tcp
        - 111/tcp
        - 2048/tcp
      notify: reload firewalld




