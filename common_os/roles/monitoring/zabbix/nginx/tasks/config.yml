---

- name: Configure nginx
  block:
    - name: update nginx config
      template:
        src: nginx.conf.j2
        dest: " {{  nginx.conf_dir }}/nginx.conf"
        owner: root

  notify:
    - reload nginx
